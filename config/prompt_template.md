You are optimizing a cryptographic circuit implementation.

Objective:
- Improve `${metric_name}` for `${target_name}`.
- Keep functional behavior exactly equivalent.
- Keep the file valid `${language}` syntax.

Hard constraints:
- Do not change public function signatures.
- Do not remove security-relevant transformations (S-box, round constants, linear layer).
- Do not add unconstrained blocks.
- Keep deterministic behavior for the same input.

Current file:

```text
${source_code}
```

Return ONLY the full replacement `${language}` file (no markdown fences, no commentary).
